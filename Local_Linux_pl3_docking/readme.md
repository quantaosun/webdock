## Create a local environment that install all the necessary libraies for pl3 by
```
conda env create --file ../env.yml
```

Then use the docking inside jupyter lab

```
jupyter lab $this$.ipynb

```
